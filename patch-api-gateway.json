{
  "spec": {
    "template": {
      "spec": {
        "containers": [
          {
            "name": "api-gateway",
            "imagePullPolicy": "Never"
          }
        ]
      }
    }
  }
}
